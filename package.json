{
  "name": "meli-challenge",
  "version": "1.0.0",
  "private": true,
  "author": "Tomás Schuster",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd meli-frontend && npm install && npm run dev",
    "dev:backend": "cd server && npm install && npm run dev",
    "build": "npm run build:frontend",
    "build:frontend": "cd meli-frontend && npm run build",
    "start": "npm run start:backend",
    "start:backend": "cd server && npm run start"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
